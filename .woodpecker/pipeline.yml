when:
  branch: develop
  event: [ push ]

steps:
  - name: docker-build
    image: docker:latest
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    commands:
      - docker build -t test-build:latest .
      - docker run --rm myapp:test echo "Build successful!"
      - echo "hey!"
    when:
      branch: develop
      event: [ push ]
